# Fase 4: nenhuma dependência externa ainda — run_agent.py só usa a
# biblioteca padrão do Python (urllib, json, hashlib, subprocess).
# A Fase 6 (Excel/PPT) e a Fase 5 (Microsoft Graph) vão adicionar
# bibliotecas aqui quando forem desenhadas.
